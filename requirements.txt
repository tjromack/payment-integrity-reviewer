# Web app
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9   # form posts from the reviewer queue

# Config
python-dotenv>=1.0

# LLM explanation layer (explanations only — never detection)
anthropic>=0.40

# Tests / formatting
pytest>=8.0
black>=24.0

# Optional secondary statistical score (Phase 2, clearly separate signal).
# Uncomment if/when the anomaly score is added:
# scikit-learn>=1.4
